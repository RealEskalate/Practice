import 'package:flutter/material.dart';

const pageBackground = Colors.white54;

const titleFontSize = 20;
const titleFontWeight = FontWeight.bold;
const titleFontColor = Colors.black;

const tagFontSize = 18;
const tagFontWeight = FontWeight.normal;
const tagFontColor = Colors.white;

const buttonBackgroundColor = Colors.blue;
const buttonContentColor = Colors.white;
const buttonHeight = 50;
const buttonWidth = 200;
const double buttonBorderRadius = 60;
const buttonTextFontWeight = FontWeight.bold;

const menuIcon = Icons.menu;
const addTagButtonIcon = Icons.add_circle_outline_outlined;
const addImageIcon = Icons.photo_camera;
const addVideoIcon = Icons.video_library;
const attachFilesIcon = Icons.attachment;
const modifyAlignmentIcon = Icons.format_align_center;
const modifyTextIcon = Icons.text_fields;
const clearIcon = Icons.clear;

const outerWidgetPadding = EdgeInsets.symmetric(horizontal: 25, vertical: 10);
const innerWidgetPadding = SizedBox(height: 10);

const inputDecorationColor = Color.fromRGBO(123, 139, 178, 0.26);
const double inputDecorationWidth = 2;
const contentPadding =
    EdgeInsets.only(left: 15, bottom: 11, top: 11, right: 15);
const double inputDecorationBorderRadius = 15;

const double hintTextSize = 19;
const hintColor = Color(0xff979EA5);

const labelColor = Colors.teal;
const double labelFontSize = 14;
